#!/usr/bin/env bash
# Compatibility shim: codex read uses the Codex App/app-server backend.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/cc-fleet-read-codex-app" "$@"

