#!/usr/bin/env bash
# Compatibility shim: codex watcher now monitors Codex App worker threads.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/cc-fleet-watch-codex-app" "$@"
